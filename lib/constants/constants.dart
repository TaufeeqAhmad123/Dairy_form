import 'package:flutter/material.dart';

const Color primaryColor = Color(0xFF39435A);

const Color greenColor = Colors.green;

const Color kBlack = Colors.black;

const double fontSize = 15.0;

const sizedBox = SizedBox(height: 10.0);

const String fontFamily = "Google Sans";

const TextStyle bodyTextStyle =
    TextStyle(fontSize: 11, color: kBlack, fontFamily: fontFamily);

const TextStyle headlineSecondaryTextStyle = TextStyle(
  letterSpacing: 1.1,
  fontSize: 10,
  color: kBlack,
  // height: 1,
  fontFamily: fontFamily,
);
const TextStyle headlineTextStyle = TextStyle(
  letterSpacing: 1.1,
  fontSize: 14,
  color: kBlack,
  height: 1,
  fontWeight: FontWeight.bold,
  fontFamily: fontFamily,
);

